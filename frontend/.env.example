# Copy to .env.local for Vite. Must match backend S3_BUCKET (R2 bucket name).
VITE_S3_BUCKET=<same-as-S3_BUCKET>

# Optional: comma-separated extra hosts whose /s3/... URLs should be rewritten to /api/s3
# VITE_ASSET_URL_HOSTS=
