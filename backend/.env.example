# Required — no defaults, must be set
JWT_SECRET=<run: openssl rand -hex 32>
ALLOWED_ORIGINS=https://riftapp.io,https://www.riftapp.io,https://staging.riftapp.io
LIVEKIT_URL=wss://livekit.riftapp.io
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret

# Optional — override Docker Compose defaults if needed
# POSTGRES_PASSWORD=riptide_dev
# MINIO_ROOT_PASSWORD=riptide_dev
